# Configuration keys

`output_dir`, `base_url`, `theme`, `cache_dir`, `minify` and `drafts`, with
their types and defaults.

The file format is described in [Configuration](../configuration.md);
`cache_dir` controls the [build cache](../architecture/cache.md).
