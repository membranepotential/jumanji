# Deploying a site

The output directory is plain files. Copy it to any web server, object store
or static host; no server-side code is needed.

Set `base_url` first; see [Configuration keys](../reference/config-keys.md).
